# R1 — nixos (2026-09-14T06:05Z)

Generated by `scripts/research/nonmutable-host.sh nixos`. Environment: **container**; uid 0; shell 5.3.9(1)-release.

## 1. What the host is


### os-release

```text
(no /etc/os-release)
```

### Update tooling present

- `rpm-ostree`: absent
- `bootc`: absent
- `dnf`: absent
- `transactional-update`: absent
- `zypper`: absent
- `snapper`: absent
- `nix`: /root/.nix-profile/bin/nix
- `nixos-rebuild`: absent
- `home-manager`: absent
- `nix-env`: /root/.nix-profile/bin/nix-env
- `flatpak`: absent
- `toolbox`: absent
- `distrobox`: absent
- `brew`: absent

### Writable?

- `/usr`: writable
- `/usr/bin`: writable
- `/etc`: writable
- `/var`: writable
- `/root`: writable

### Escalator and login-shell machinery

- `sudo`: absent
- `doas`: absent
- `chsh`: absent
- `usermod`: absent
- `getent`: absent
- `zsh`: absent
- `git`: /root/.nix-profile/bin/git
- `/etc/shells`: absent
- `/etc/passwd` writable: yes

### The update verbs, as the schema would call them

- **nix --version**: `nix --version` → exit 0 — `nix (Nix) 2.35.2`
- **nixos-version**: `nixos-version` → exit 127 — `scripts/research/nonmutable-host.sh: line 86: nixos-version: command not found`
- **nix-env --version**: `nix-env --version` → exit 0 — `nix-env (Nix) 2.35.2`
- **home-manager --version**: `home-manager --version` → exit 127 — `scripts/research/nonmutable-host.sh: line 86: home-manager: command not found`
- **ls /run/current-system/sw/bin**: `ls /run/current-system/sw/bin` → exit 2 — `ls: cannot access '/run/current-system/sw/bin': No such file or directory`
- **ls ~/.nix-profile/bin**: `ls /root/.nix-profile/bin` → exit 0 — `[`

## 2. The repo

No `dotfiles-NixOS` exists; scaffolding one with `scripts/new-os-repo.sh --no-vendor NixOS` and seeding THIS Core as its `core/` (the same seeding `scripts/test/35-new-os-repo.sh` does).
**scaffold** — `env -u CORE_JSON bash /w/scripts/new-os-repo.sh --no-vendor NixOS /tmp/nonmutable.sBIRWq/dotfiles-NixOS` → exit **0**

```text

== scaffold dotfiles-NixOS ==
:: target   = /tmp/nonmutable.sBIRWq/dotfiles-NixOS
:: core     =  (refs/tags/v7)
– skipping vendor (--no-vendor) — run later: git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS add -A && (git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS diff --cached --quiet || git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS commit -q -m scaffold\ dotfiles-NixOS) && { git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS cat-file -e HEAD:core 2>/dev/null || git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS subtree add --prefix=core "${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" refs/tags/v7 --squash; } && (cd /w && git fetch "${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" refs/tags/v7 && _wtp="$(mktemp -d "${TMPDIR:-/tmp}/dotfiles-core-sync.XXXXXX")" && _wt="$_wtp/core" && { git worktree add --detach "$_wt" FETCH_HEAD || { rmdir "$_wtp"; false; }; } && { _o="$( (cd "$_wt" && CORE_BRANCH="$(git rev-parse 'HEAD^{commit}')" CORE_REMOTE="${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" CORE_COLOR=never REPOS_ROOT=/tmp/nonmutable.sBIRWq ./scripts/sync-core.sh dotfiles-NixOS) 2>&1)" || true; printf '%s\n' "$_o"; _l="$(awk '/^ *repos: /{l=$0} END{print l}' <<<"$_o")"; if grep -Eq '^ *repos: +updated 1 +skipped 0 +failed 0 +\(of 1 targeted\)$' <<<"$_l"; then _rc=0; else _rc=1; fi; git worktree remove --force "$_wt" && rmdir "$_wtp" && exit "$_rc"; })   # VENDORING.md, One-time setup
✓ wrote zsh/zshenv.zsh
✓ wrote zsh/zprofile.zsh
✓ wrote os/nixos.zsh
✓ wrote os/nixos.capabilities
✓ wrote bootstrap.sh
✓ wrote .gitignore
✓ wrote Makefile
✓ wrote .markdownlint.jsonc
✓ wrote test/check-links.sh
✓ wrote .github/workflows/lint.yml
✓ wrote .github/workflows/test.yml
✓ wrote .github/workflows/auto-tag.yml
✓ wrote README.md

──────── dotfiles-NixOS scaffolded ────────
  next:
  FIRST — this scaffold has no core/ yet, and bootstrap.sh refuses to run without one,
  so nothing below passes until it exists. Materialize it (this commits the scaffold,
  then adds core/ and runs the pinned sync that stamps core.lock):
    git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS add -A && (git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS diff --cached --quiet || git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS commit -q -m scaffold\ dotfiles-NixOS) && { git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS cat-file -e HEAD:core 2>/dev/null || git -C /tmp/nonmutable.sBIRWq/dotfiles-NixOS subtree add --prefix=core "${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" refs/tags/v7 --squash; } && (cd /w && git fetch "${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" refs/tags/v7 && _wtp="$(mktemp -d "${TMPDIR:-/tmp}/dotfiles-core-sync.XXXXXX")" && _wt="$_wtp/core" && { git worktree add --detach "$_wt" FETCH_HEAD || { rmdir "$_wtp"; false; }; } && { _o="$( (cd "$_wt" && CORE_BRANCH="$(git rev-parse 'HEAD^{commit}')" CORE_REMOTE="${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" CORE_COLOR=never REPOS_ROOT=/tmp/nonmutable.sBIRWq ./scripts/sync-core.sh dotfiles-NixOS) 2>&1)" || true; printf '%s\n' "$_o"; _l="$(awk '/^ *repos: /{l=$0} END{print l}' <<<"$_o")"; if grep -Eq '^ *repos: +updated 1 +skipped 0 +failed 0 +\(of 1 targeted\)$' <<<"$_l"; then _rc=0; else _rc=1; fi; git worktree remove --force "$_wt" && rmdir "$_wtp" && exit "$_rc"; })   # VENDORING.md, One-time setup

    cd "/tmp/nonmutable.sBIRWq/dotfiles-NixOS"
    git add -A && { git diff --cached --quiet || git commit -m "scaffold dotfiles-NixOS"; }   # a no-op if already committed
    ./bootstrap.sh            # wire the symlinks
    make test                 # the repo's own suite (the fleet's test floor)
    $EDITOR os/nixos.zsh     # add your NixOS-native bits

  THEN FIX THE CAPABILITY DECLARATION — it was scaffolded with FEDORA's verbs so the
  schema is satisfied from birth, and they are wrong for any other archive:
    $EDITOR os/nixos.capabilities
    core/scripts/check-capabilities.sh os/nixos.capabilities
    git add -A && git commit -m "os: the NixOS layer and its capability declaration"   # the sync below refuses a dirty tree

  then, back in dotfiles-core — REGISTER IT, or the fleet never sees this repo:
    echo dotfiles-NixOS >> scripts/os-repos.txt   # one line; keep the list sorted
    (cd /w && git fetch "${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" refs/tags/v7 && _wtp="$(mktemp -d "${TMPDIR:-/tmp}/dotfiles-core-sync.XXXXXX")" && _wt="$_wtp/core" && { git worktree add --detach "$_wt" FETCH_HEAD || { rmdir "$_wtp"; false; }; } && { _o="$( (cd "$_wt" && CORE_BRANCH="$(git rev-parse 'HEAD^{commit}')" CORE_REMOTE="${CORE_REMOTE:?export CORE_REMOTE=<dotfiles-core remote URL> first}" CORE_COLOR=never REPOS_ROOT=/tmp/nonmutable.sBIRWq ./scripts/sync-core.sh dotfiles-NixOS) 2>&1)" || true; printf '%s\n' "$_o"; _l="$(awk '/^ *repos: /{l=$0} END{print l}' <<<"$_o")"; if grep -Eq '^ *repos: +updated 1 +skipped 0 +failed 0 +\(of 1 targeted\)$' <<<"$_l"; then _rc=0; else _rc=1; fi; git worktree remove --force "$_wt" && rmdir "$_wtp" && exit "$_rc"; })   # the PINNED sync (a throwaway worktree at the release ref; its summary line is the verdict): stamps core.lock — a custom name gets the guarded symlink first, and a refusal stops it
```
- seeded core/ from Core at `?` (core.version 7.4.3)
- bootstrap.sh hands over to the driver: yes

## 3. Probe-only paths (throwaway HOME, BLIB_SU= as root)

**--help** — `env HOME=/tmp/nonmutable.sBIRWq/home XDG_CONFIG_HOME=/tmp/nonmutable.sBIRWq/home/.config BLIB_SU= ./bootstrap.sh --help` → exit **0**

```text
usage: bootstrap.sh [flags]

Wire the vendored Core + the NixOS os/ layer into place. Idempotent; safe to re-run
after every Core sync. This starter provisions nothing yet, so --links-only is what
every run does; the shared flags below are the driver's.

Shared flags (core/lib/bootstrap-lib.sh :: blib_main):
  --links-only        (re)create the symlinks only — no provisioning, no host probe
  --dry-run, -n       print every planned change and change nothing; provisioning is
                      skipped rather than faked
  --strict            exit 1 if any best-effort step did not complete (misses are
                      listed either way)
  --only=G,G          wire ONLY these Core module groups   (zsh nvim tmux git prompt tools)
  --skip=G,G          wire everything EXCEPT these groups
  -h, --help          this text
```
**--links-only** — `env HOME=/tmp/nonmutable.sBIRWq/home XDG_CONFIG_HOME=/tmp/nonmutable.sBIRWq/home/.config BLIB_SU= ./bootstrap.sh --links-only` → exit **0**

```text
:: symlinking Core zsh modules
:: seeded /tmp/nonmutable.sBIRWq/home/.config/git/local.gitconfig — FILL IN your name & email
:: seeded /tmp/nonmutable.sBIRWq/home/.config/mise/config.toml — mise rewrites this file; yours to edit
:: symlinking nixos OS-native layer
:: writing .zshrc loader
ok core-guard: pre-commit installed in dotfiles-NixOS
ok 28 linked · 2 seeded · 0 backed up · 0 relinked · 0 skipped
ok NixOS bootstrap complete — open a new shell, or: exec zsh
```
- links made under the throwaway HOME: 28; managed ~/.zshrc: yes
**--dry-run** — `env HOME=/tmp/nonmutable.sBIRWq/home-dry XDG_CONFIG_HOME=/tmp/nonmutable.sBIRWq/home-dry/.config BLIB_SU= ./bootstrap.sh --dry-run` → exit **0**

```text
:: symlinking Core zsh modules
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/00-tools.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/02-capabilities.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/05-ui.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/10-options.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/15-history.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/20-aliases.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/25-git.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/30-functions.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/35-fzf.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/40-bindings.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/45-plugins.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/50-op.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/55-maint.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/60-update.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/loader.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/nvim
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/tmux/tmux.conf
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/tmux/tmux.reset.conf
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/tmux/scripts
:: would clone tpm (tmux plugin manager)
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/starship.toml
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.gitconfig
:: would seed: /tmp/nonmutable.sBIRWq/home-dry/.config/git/local.gitconfig (FILL IN your name & email)
:: would seed: /tmp/nonmutable.sBIRWq/home-dry/.config/mise/config.toml (mise rewrites this file; yours to edit)
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.local/bin/clip
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.local/bin/clip-paste
:: symlinking nixos OS-native layer
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/80-os.zsh
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/os.capabilities
:: would write managed ~/.zshrc loader (v4 numbered-fragment glob)
:: would seed /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/.zshrc -> ~/.zshrc
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.zshenv
:: would link: /tmp/nonmutable.sBIRWq/home-dry/.config/zsh/.zprofile
:: (dry run) would install the core/ pre-commit guard
ok (dry run) 27 linked · 2 seeded · 0 backed up · 0 relinked · 0 skipped
ok NixOS dry run complete — nothing was changed; re-run without --dry-run to apply
```
- --dry-run wrote into HOME: nothing

## 4. The real run (this is the measurement)

Run as root with `BLIB_SU=` so the escalator resolves to nothing; the provisioning verb is whatever the repo declares. On a container this is the image-build context, not a booted host — read §1 before trusting a green here.
**full run** — `env HOME=/tmp/nonmutable.sBIRWq/home-real XDG_CONFIG_HOME=/tmp/nonmutable.sBIRWq/home-real/.config BLIB_SU= ./bootstrap.sh` → exit **0**

```text
:: symlinking Core zsh modules
:: seeded /tmp/nonmutable.sBIRWq/home-real/.config/git/local.gitconfig — FILL IN your name & email
:: seeded /tmp/nonmutable.sBIRWq/home-real/.config/mise/config.toml — mise rewrites this file; yours to edit
:: symlinking nixos OS-native layer
:: writing .zshrc loader
ok 28 linked · 2 seeded · 0 backed up · 0 relinked · 0 skipped
ok NixOS bootstrap complete — open a new shell, or: exec zsh
```
- login shell now: 
- /etc/shells now: absent
- tools on PATH after the run: git 

## 5. Reading the run against §3's table

Fill in by hand after reading the sections above — the script records, the reader concludes:

| Assumption | Held / broke here | Note |
| --- | --- | --- |
| PKG_INSTALL is synchronous | | |
| PKG_UPGRADE returns with the box updated | | |
| PKG_COUNT_PENDING lists packages | | |
| /etc writable (chsh, /etc/shells, system files) | | |
| Escalator + keepalive | | |
| Tools on PATH by package | | |
| --links-only holds unchanged | | |
